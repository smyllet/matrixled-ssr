services:
  postgres:
    image: postgres:18-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-matrixled}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-matrixled}
      POSTGRES_DB: ${DB_DATABASE:-matrixled}
    ports:
      - '${DB_PORT:-5432}:5432'
    volumes:
      - postgres-data:/var/lib/postgresql
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${DB_USER:-matrixled} -d ${DB_DATABASE:-matrixled}']
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres-data:
